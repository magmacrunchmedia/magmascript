# MagmaScript

## GitHub Account

Always use the `magmacrunchmedia` GitHub account for any git/gh operations.
NEVER use `jake-chromatir` for anything.

## Language

MagmaScript is a Python-inspired mini language with `.mgs` file extension.
Tree-walk interpreter with domain bridge to magmascript modules.
